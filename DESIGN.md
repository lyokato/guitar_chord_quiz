# DESIGN.md

ギターコード・ポジションクイズのデザインシステム。UIを生成・変更する際は必ずこのファイルに従うこと。

## Visual Theme & Atmosphere

ダークテーマ固定の、落ち着いた練習ツール。ゲーム的な派手さや装飾は不要。
主役は常に「コード情報」と「指板グラフィック」であり、ナビゲーションや補助UIはそれを食ってはいけない。
説明的なテキストは最小限に(「見れば分かるものに領域を使わない」— 弦ラベル・凡例は過去に削除済み)。
UIは日本語。ただしページ名・アクション名は日本語(カタカナ)表記: ギター練習ツール、コードクイズ、リズムボックス、スタート、コンフィグ。

## Color Palette

すべてCSS変数で定義済み。**定義外の色を直接書かないこと。**

| 変数 | 値 | 用途 |
|---|---|---|
| `--bg` | #14171c | 画面背景 |
| `--panel` | #1e232b | カード背景(問題カード・指板ブロック・設定パネル) |
| `--panel2` | #262c36 | チップ・入力・ボタンの背景 |
| `--text` | #e8ecf1 | 本文 |
| `--sub` | #9aa5b1 | 補足テキスト・フレット番号等の弱い情報 |
| `--accent` | #4fc3f7 | キーカラー: コードネーム表示・見出し・START・プリセットボタン・チェックボックス |
| `--nav` | #2e4166 | ナビゲーションボタン専用(進む・終了・閉じる)。暗めのネイビーで控えめに |
| `--timer` | #ffb74d | タイマー機能群専用(カウンタリング・一時停止・再開) |
| `--root` | #ef5350 | 指板上のルート音・ルート基準マーク |
| `--tone` | #26a69a | 指板上の構成音マーク、音を鳴らすボタン背景(音とポジションの対応を色で示唆) |
| `--line` | #3a424e | 枠線・区切り線・disabled状態 |

**色の規律(重要)**: 機能群と色は1対1で対応させる。
タイマー関連=必ず`--timer`、画面遷移=必ず`--nav`。`--accent`(青)は強すぎるためナビゲーションには使わない(過去に却下済みの決定)。

## Typography

- フォント: `"Hiragino Sans", "Noto Sans JP", "Yu Gothic", sans-serif`
- コード記号・コードネームチップ: `Menlo, Consolas, monospace` + 白太字
- コードネーム(問題カード): 4.2rem / 800 / `--accent`(スマホ縦2.8rem、横持ち2rem)
- 問題情報行(ルートモード/構成): 1.25rem / 白 #fff(グレー不可 — 視認性のため白に変更済み)
- 画面タイトル(h1): 1.9rem、ナビバータイトル: 1.3rem / 700 / 中央配置
- セクション見出し(h2): 1.2rem / `--accent`
- 注釈(.note): 0.8rem / `--sub`
- カウントダウン数字: 1.7rem / 800(両フェーズ同一)

## Components

### ボタン

全アクションは円形アイコンボタン(`.icon-btn`)+**インラインSVGアイコン**。画像素材・絵文字アイコンは使わない。

| 種別 | クラス | 見た目 | サイズ | 用途 |
|---|---|---|---|---|
| ナビゲーション | `.icon-btn.nav` | `--nav`地 + 淡色アイコン #d9e3f3 | 60px / 横持ち52px | 進む(右Chevron)、戻る(左Chevron) |
| タイマー操作 | `.icon-btn.timer` | 透明地 + `--timer` 2px枠 | 64px / 横持ち50px | ⏸ / ▶。カウンタ(72px)より**わずかに小さく** |
| 補助操作 | `.icon-btn.secondary.float-play` | `--tone`地 + 白アイコン + 影 | 48px | 音を鳴らす(音符)。指板ブロック右上にフローティング |
| テキストボタン | `.primary` / `.ghost` | 青地 / 枠線のみ | big=1.25rem | タイトル画面のスタート/コンフィグのみ |
| プリセット | `.preset-btns button` | 青1.5px枠 + 淡青地(14%) + 青文字 | - | レッスン一括設定。パネル背景に溶けないこと |
| 小ボタン | `.small` | `--panel2`地 | .85rem | 全選択/全解除など補助操作 |

- ⏸と▶はトグル化しない。**常に両方表示**し、disabled(グレー枠)で状態を示す(幅のガタつき防止)
- 「答えを見る」と「次へ」は同一役割(画面進行)なので**1つの共通ボタン**(右Chevron)
- 前の画面に戻る操作は**画面左上の左Chevronボタンに統一**(全画面共通)。×ボタンは使わない。**全画面で同一サイズ(60px/横持ち52px)・同一位置(コンテンツ左上、上端揃え)**にすること
- 各ツールのタイトル画面はホーム(🎸絵文字ロゴ)と被らない固有のアートを置く(コードクイズ=指板SVG)

### タイマーユニット

カウンタリング(72px、`--timer`色、90 viewBox SVG)+⏸+▶を10px間隔で密に隣接させ、ひとつの機能群として見せる。
カウンタは回答待ち/回答表示の**両フェーズで同一の1個**を使い、サイズ・位置が遷移で跳ねてはいけない。

### カード・パネル

- 問題カード(.q-card): `--panel`地、radius 16px、中央揃え、2行構成(情報行+コードネーム)
- 設定パネル(.panel): `--panel`地、radius 14px
- フィルターカード(.filter-group): 白3%地 + `--line`枠、radius 10px
- 選択チップ(.chk-group label): `--panel2`地、選択中はグレー半透明枠 `rgba(154,165,177,.45)`(青枠は不可)。チェックボックス自体は`--accent`(チェック状態の視認が最優先)

### RHYTHM BOX固有

- **1画面完結を守る**: スクロールせずに再生ボタンまで見えること。設定はセレクトボックス(`.cfg-row select`: `--panel2`地+`--line`枠)でコンパクトに
- 再生/停止ボタン(.play-btn): `--tone`地+白アイコンの大きな円形(84px)。▶⇔■は同サイズの円形トグル(幅が変わらないため許容)
- ビートインジケーター(.beat-dots): 拍数分のドット。点灯は`--timer`、1拍目のアクセントのみ`--accent`
- BPM数値は1.4remの青太字(コンパクト優先)、スライダーは`accent-color: var(--accent)`

### 指板グラフィック(SVG)

- 横向き・ナット左・16フレット・1弦が上。弦ラベル・凡例は描かない
- マーク: ルート=`--root`丸、構成音=`--tone`丸(r=13)。丸内ラベルは設定で度数(R/3/♭7、デフォルト)⇔音名
- ルート基準位置(弾かない弦)=`--root`点線丸+「R」
- バレー=人差し指シルエット(#e0ac69 半透明の縦バー+指先の丸)
- ミュート弦=ナット左に「×」(`--sub`)
- フレット番号(#c2cad4/15.5px/600)とインレイ(#6e6350)は「ポジションマークより控えめ、だが位置認知できる」明るさ
- 収まらない場合: ルート音が中央に来るよう自動スクロール+トリム側の端にグラデーションフェード(`.edge-fade`)+Chevron表示

## Layout & Spacing

- コンテンツ幅: max-width 980px 中央寄せ。フローティング要素も**ビューポートでなくコンテンツ幅の右端**に揃える(画面右下固定FABは却下済み)
- クイズ画面の構成(両フェーズ共通):
  `ヘッダー(← 戻る)` → `問題カード` → `ナビゲーションライン` → (回答時のみ)`指板`
  フェーズ遷移で上部ブロックのレイアウトは**一切動かず**、下に指板が増えるだけ。問題番号などの付加情報は表示しない
- ナビゲーションライン(.control-line): タイマー群を中央、進むボタンを絶対配置で右端。min-height 72px(タイマー非表示時もボタン位置を維持)
- コンフィグ/リズムボックスはスティッキーなナビバー(戻るChevron左端・タイトル中央)。スクロール位置に関わらず戻れること
- 数値入力には `inputmode="numeric" pattern="[0-9]*"` を付ける

### レスポンシブ

- `max-width: 600px`(スマホ縦): 文字・チップ縮小。指板は縮小せず**横スクロール**(SVG min-width 640px)で視認性を守る
- `max-height: 520px + landscape`(スマホ横): 余白・フォント・リング径(56px)を圧縮して1画面に収める

## Do's and Don'ts

### Do's
- 色は必ずCSS変数から選ぶ。新しい機能群を作るときは専用トークンを追加してから使う
- アイコンが必要なら24 viewBoxのインラインSVGを書く(stroke: currentColor)
- 同じ役割のボタンは同じデザイン・同じサイズに揃える
- 情報の強弱: 主役(コード名・ポジション) > 位置手がかり(フレット番号・インレイ) > 補助(注釈)

### Don'ts
- `--accent`をナビゲーションボタンに使わない
- タイマー系以外に`--timer`(アンバー)を使わない
- トグルでボタンの幅・位置が変わるUIを作らない
- 画面遷移時に既存ブロックのレイアウトを動かさない
- 説明テキスト・凡例・ラベルを安易に追加しない(まず「見れば分かるか」を考える)
- 絵文字をアイコンとして使わない(タイトル画面の🎸ロゴのみ例外)
